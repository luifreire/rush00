/** tem que fazer aparecer um RETÂNGULO MANO  
 * um retângulo em que o x é a largura 
 * e o y é a altura 
 * não pode crashar nem ficar de loop INDEFINIDO!
 *
 * função com 2 variáveis do tipo int, nomes x e y 
 */
 
 #include <stdio.h>

 void main()
 {
    int fd();

    Char l[i] = "l"*i;

    Char o[j] = "o"*j;
    
    Char pula[i] = "\n"*i;

        Fd[] = open("Documentos/42trabalhos/rush.00/rush00.c", O_RDONLY);

        for (int i = 0; i < 5; i++)
        {
            for (int j = 0; j < 3; j++)
            {

            write(i, &l, i)
    
            write(j, &o, j)
            }
            write(i, &\n, i);
        }

    close(fd[]);
    
 }
